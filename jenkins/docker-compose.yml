version: '3'
services:
  jenkins_container:
    build: .
    ports:
      - 8080:8080
    volumes:
      - jenkins-data:/var/jenkins_home
    environment:
      - JENKINS_ADMIN_ID=admin
      - JENKINS_ADMIN_PASSWORD=password
volumes:
  jenkins-data:
